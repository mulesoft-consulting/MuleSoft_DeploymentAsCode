version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run: ls -la /home/circleci/project
      - restore_cache:
          keys:
            - npm-cache-{{ .Branch }}-{{ checksum "node_modules/js-yaml/package.json" }}
      - restore_cache:
          keys:
            - anypointcli-cache-{{ .Branch }}-{{ checksum "/usr/local/lib/node_modules/anypoint-cli/package.json" }}
      - run: sudo npm install -g anypoint-cli
      - run: sudo npm install js-yaml
      - save_cache:
          key: npm-cache-{{ .Branch }}-{{ checksum "node_modules/js-yaml/package.json" }}
          paths:
            - node_modules
      - save_cache:
          key: anypointcli-cache-{{ .Branch }}-{{ checksum "/usr/local/lib/node_modules/anypoint-cli/package.json" }}
          paths:
            - /usr/local/lib/node_modules/anypoint-cli
      - deploy:
          name: Deployment
          command: | 
            chmod +x .muleci/deployment.sh;
            .muleci/deployment.sh deployment_descriptor.yml;

# deployment:
#   production: # just a label; label names are completely up to you
#     branch: master
#     commands:
        #- ./deploy-to-production.sh
#        - echo "deployment prod"
