version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - restore_cache:
          keys:
            - npm-cache-{{ .Branch }}-{{ checksum ".circleci/cache.lock" }}
      - run: sudo npm install anypoint-cli
      - run: sudo npm install js-yaml
      - run: sudo ln -s node_modules/anypoint-cli/src/app.js /usr/local/bin/anypoint-cli
      - save_cache:
          key: npm-cache-{{ .Branch }}-{{ checksum ".circleci/cache.lock" }}
          paths:
            - node_modules
      - deploy:
          name: Deployment
          command: | 
            chmod +x .muleci/deployment.sh;
            .muleci/deployment.sh deployment_descriptor.yml;
